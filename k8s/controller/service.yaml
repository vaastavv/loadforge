apiVersion: v1
kind: Service
metadata:
  name: controller
  labels:
    app: controller
    app.kubernetes.io/part-of: loadforge
spec:
  type: ClusterIP
  selector:
    app: controller
  ports:
    - name: http
      port: 8080
      targetPort: 8080
