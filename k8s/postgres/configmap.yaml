apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  labels:
    app: postgres
    app.kubernetes.io/part-of: loadforge
data:
  POSTGRES_DB: loadforge
  POSTGRES_USER: postgres
  # Keep data in a subdirectory so the mount root stays clean.
  PGDATA: /var/lib/postgresql/data/pgdata
