apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  labels:
    app: postgres
    app.kubernetes.io/part-of: loadforge
type: Opaque
stringData:
  # Local-dev credential only. Replace before using anywhere real.
  POSTGRES_PASSWORD: postgres
