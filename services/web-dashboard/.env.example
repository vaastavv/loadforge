# Base URL of the LoadForge control-plane API.
# Leave empty to call the API on the same origin (recommended for local dev — Vite proxies /api).
VITE_API_BASE_URL=

# Dev-only: target the Vite dev server proxies /api requests to.
VITE_PROXY_TARGET=http://localhost:8080
